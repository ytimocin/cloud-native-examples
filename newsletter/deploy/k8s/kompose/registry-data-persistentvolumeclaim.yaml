apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: registry-data
  name: registry-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
